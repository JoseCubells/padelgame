namespace Padel.Rules
{
    /// <summary>
    /// Half of the court the serve is delivered from, seen by the server facing the net.
    /// Right is the "drive" side, where every game starts (FIP S4).
    /// </summary>
    public enum ServeSide
    {
        Right = 0,
        Left = 1,
    }
}
